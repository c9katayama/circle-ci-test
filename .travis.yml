sudo: false
language: java
jdk:
  - oraclejdk8
install:
 - apt-get install python
 - pip install awsebcli
before_script:
 - chmod +x build.sh
branches:
  only:
    - master
    - prod-deploy
    - staging-deploy
script: ./build.sh
after_success:
  - echo "hello"